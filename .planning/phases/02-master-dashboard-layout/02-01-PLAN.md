---
wave: 1
depends_on: []
files_modified:
  - "src/components/dashboard/BentoGrid.tsx"
  - "src/components/dashboard/WidgetWrapper.tsx"
  - "src/pages/Dashboard.tsx"
autonomous: true
---

# Plan 02-01: Develop strict Bento Grid component wrapper architecture

## Objective
Implement the Bento Grid layout and Widget Wrapper components to establish the core dashboard architecture.

## Requirements Addressed
- DASH-03

## must_haves
- BentoGrid component that uses Tailwind CSS Grid to layout children.
- WidgetWrapper component that includes a standardized header and action menu.
- Dashboard page renders the BentoGrid.
- D-01: Use a static preset layout.
- D-03: Use standardized titles + action menus.

## Tasks

<task>
<id>02-01-01</id>
<title>Create WidgetWrapper component</title>
<action>Create `WidgetWrapper` in `src/components/dashboard/WidgetWrapper.tsx`. It should accept `title`, `children`, and optional `className`. The header must include the title and a Lucide React `MoreHorizontal` icon for the action menu. Use `theme-surface` for background.</action>
<read_first>
- c:\Users\misha\ledger\.planning\phases\02-master-dashboard-layout\02-UI-SPEC.md
</read_first>
<acceptance_criteria>
- `WidgetWrapper.tsx` exports a React component.
- The component renders an `h2` with the provided title and an action icon.
- Vitest unit test passes.
</acceptance_criteria>
</task>

<task>
<id>02-01-02</id>
<title>Create BentoGrid layout component</title>
<action>Create `BentoGrid` in `src/components/dashboard/BentoGrid.tsx` using Tailwind grid classes (`grid`, `gap-lg`, responsive column definitions). Update `src/pages/Dashboard.tsx` to use it.</action>
<read_first>
- c:\Users\misha\ledger\.planning\phases\02-master-dashboard-layout\02-UI-SPEC.md
</read_first>
<acceptance_criteria>
- `BentoGrid` uses `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` or `4` with `gap-6` (lg spacing).
- `Dashboard.tsx` imports and renders `BentoGrid`.
</acceptance_criteria>
</task>

## Artifacts this phase produces
- `src/components/dashboard/BentoGrid.tsx`
- `src/components/dashboard/WidgetWrapper.tsx`
