---
wave: 2
depends_on:
  - "02-01"
files_modified:
  - "src/store/useDashboardStore.ts"
  - "src/components/dashboard/widgets/NetWorthWidget.tsx"
  - "src/components/dashboard/widgets/AssetsWidget.tsx"
  - "src/components/dashboard/widgets/DebtsWidget.tsx"
  - "src/pages/Dashboard.tsx"
autonomous: true
---

# Plan 02-02: Build Net Worth, assets, and liabilities widgets with local state

## Objective
Implement local Zustand state for dashboard data and create the widgets for Net Worth, Assets, and Debts to populate the Bento Grid.

## Requirements Addressed
- DASH-01
- DASH-02

## must_haves
- `useDashboardStore.ts` manages mock Net Worth, Assets, and Debts data.
- `NetWorthWidget` displays total net worth and trend percentage.
- `AssetsWidget` and `DebtsWidget` display overviews.
- D-02: Use a simple number + trend percentage for Net Worth.

## Tasks

<task>
<id>02-02-01</id>
<title>Implement Dashboard Zustand store</title>
<action>Create `src/store/useDashboardStore.ts` following `useThemeStore.ts` patterns. Define state interface for `netWorth`, `netWorthTrend`, `totalAssets`, `totalDebts`, and set up initial mock data.</action>
<read_first>
- src/store/useThemeStore.ts
</read_first>
<acceptance_criteria>
- `useDashboardStore` is exported and returns the correct state schema.
- Mock data is populated with non-zero values.
</acceptance_criteria>
</task>

<task>
<id>02-02-02</id>
<title>Create NetWorthWidget</title>
<action>Create `NetWorthWidget` in `src/components/dashboard/widgets/NetWorthWidget.tsx` using `WidgetWrapper`. It should read from `useDashboardStore` and display the net worth and trend. Use `text-36px` (Display) for the number.</action>
<read_first>
- c:\Users\misha\ledger\.planning\phases\02-master-dashboard-layout\02-UI-SPEC.md
- src/components/dashboard/WidgetWrapper.tsx
</read_first>
<acceptance_criteria>
- Component renders Net Worth amount and trend.
- Trend text uses `theme-accent` color.
</acceptance_criteria>
</task>

<task>
<id>02-02-03</id>
<title>Create Assets and Debts Widgets</title>
<action>Create `AssetsWidget` and `DebtsWidget` in `src/components/dashboard/widgets/`. Render them inside `BentoGrid` in `Dashboard.tsx` alongside `NetWorthWidget`.</action>
<read_first>
- src/components/dashboard/widgets/NetWorthWidget.tsx
</read_first>
<acceptance_criteria>
- Widgets render total assets and debts respectively.
- `Dashboard.tsx` integrates all three widgets.
</acceptance_criteria>
</task>

## Artifacts this phase produces
- `src/store/useDashboardStore.ts`
- `src/components/dashboard/widgets/NetWorthWidget.tsx`
- `src/components/dashboard/widgets/AssetsWidget.tsx`
- `src/components/dashboard/widgets/DebtsWidget.tsx`
