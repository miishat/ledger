---
wave: 2
depends_on: ["03-01"]
files_modified:
  - "src/components/budget/CashFlowWidget.tsx"
  - "src/components/budget/IncomeWidget.tsx"
  - "src/components/budget/ExpenseWidget.tsx"
  - "src/pages/Dashboard.tsx"
autonomous: true
---

# Plan 03-02: Develop categorized expense views and cash flow summaries.

## Objective
Build the dashboard widgets for viewing income, categorized expenses, and the monthly cash flow summary.

## Requirements Addressed
- BUDGET-01
- BUDGET-02
- BUDGET-03

## must_haves
- `CashFlowWidget` calculates and displays `totalIncome - totalExpenses` for the current month.
- `IncomeWidget` displays total income for the current month.
- `ExpenseWidget` displays categorized expenses for the current month.
- Widgets utilize the `WidgetWrapper` created in Phase 2.
- Widgets are integrated into the main `Dashboard.tsx` Bento Grid.

## Tasks

<task>
<id>03-02-01</id>
<title>Create IncomeWidget</title>
<action>Create `IncomeWidget` in `src/components/budget/IncomeWidget.tsx`. Use `useBudgetStore` to aggregate transactions of type 'income' for the current month. Render the total using `WidgetWrapper` with title "Income".</action>
<read_first>
- c:\Users\misha\ledger\.planning\phases\03-budgeting-module\03-RESEARCH.md
- c:\Users\misha\ledger\src\components\dashboard\WidgetWrapper.tsx
</read_first>
<acceptance_criteria>
- `IncomeWidget.tsx` exports a React component.
- Displays the correct aggregated income sum for the current month.
</acceptance_criteria>
</task>

<task>
<id>03-02-02</id>
<title>Create ExpenseWidget</title>
<action>Create `ExpenseWidget` in `src/components/budget/ExpenseWidget.tsx`. Use `useBudgetStore` to aggregate transactions of type 'expense' for the current month, grouped by category. Render using `WidgetWrapper` with title "Expenses".</action>
<read_first>
- c:\Users\misha\ledger\.planning\phases\03-budgeting-module\03-RESEARCH.md
- c:\Users\misha\ledger\src\components\dashboard\WidgetWrapper.tsx
</read_first>
<acceptance_criteria>
- `ExpenseWidget.tsx` exports a React component.
- Displays categorized expenses correctly.
</acceptance_criteria>
</task>

<task>
<id>03-02-03</id>
<title>Create CashFlowWidget and integrate into Dashboard</title>
<action>Create `CashFlowWidget` in `src/components/budget/CashFlowWidget.tsx`. Calculate `totalIncome - totalExpenses` for the current month. Render using `WidgetWrapper`. Finally, add all three widgets to `src/pages/Dashboard.tsx` inside the Bento Grid.</action>
<read_first>
- c:\Users\misha\ledger\.planning\phases\03-budgeting-module\03-RESEARCH.md
- c:\Users\misha\ledger\src\components\dashboard\WidgetWrapper.tsx
- c:\Users\misha\ledger\src\pages\Dashboard.tsx
</read_first>
<acceptance_criteria>
- `CashFlowWidget` displays the net difference correctly.
- `Dashboard.tsx` includes `IncomeWidget`, `ExpenseWidget`, and `CashFlowWidget`.
</acceptance_criteria>
</task>

## Artifacts this phase produces
- `src/components/budget/CashFlowWidget.tsx`
- `src/components/budget/IncomeWidget.tsx`
- `src/components/budget/ExpenseWidget.tsx`
