---
wave: 1
depends_on: []
files_modified:
  - "src/store/useBudgetStore.ts"
  - "src/components/budget/TransactionModal.tsx"
autonomous: true
---

# Plan 03-01: Build data entry forms and local state schema for budgeting.

## Objective
Create the local state schema using Zustand for managing income and expenses, and build the data entry modal overlay for users to manually input their transactions.

## Requirements Addressed
- BUDGET-01
- BUDGET-02
- BUDGET-03

## must_haves
- D-01: A `TransactionModal` component to capture `amount`, `category`, and `date`.
- D-02: Fixed categories defined in the application (e.g., Housing, Food, Transport).
- D-03: Modal inputs dispatch updates correctly to the state.
- A `zustand` store (`useBudgetStore.ts`) to manage income and expense lists.

## Tasks

<task>
<id>03-01-01</id>
<title>Create useBudgetStore</title>
<action>Create `useBudgetStore` in `src/store/useBudgetStore.ts`. Define the `Transaction` type (`id`, `type` ('income' or 'expense'), `amount`, `category`, `date`, `description`). Define actions to `addTransaction` and `deleteTransaction`. Export standard categories like 'Housing', 'Food', 'Transport', 'Utilities', 'Entertainment', 'Other'.</action>
<read_first>
- c:\Users\misha\ledger\.planning\phases\03-budgeting-module\03-RESEARCH.md
</read_first>
<acceptance_criteria>
- `useBudgetStore.ts` exports a Zustand store.
- Type definitions for `Transaction` are present.
- Store has initial state of empty transactions array.
</acceptance_criteria>
</task>

<task>
<id>03-01-02</id>
<title>Build TransactionModal overlay</title>
<action>Create `TransactionModal` in `src/components/budget/TransactionModal.tsx`. It should contain a form with inputs for type (income/expense), amount, date, category (dropdown using fixed categories), and description. On submit, it calls `addTransaction` from `useBudgetStore`. Follow the UI design contract for colors and spacing.</action>
<read_first>
- c:\Users\misha\ledger\.planning\phases\03-budgeting-module\03-UI-SPEC.md
</read_first>
<acceptance_criteria>
- `TransactionModal.tsx` exports a React component.
- The modal renders the correct form fields.
- Form submission updates the `useBudgetStore` state.
</acceptance_criteria>
</task>

## Artifacts this phase produces
- `src/store/useBudgetStore.ts`
- `src/components/budget/TransactionModal.tsx`
