---
phase: 06-total-compensation-calculator
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/components/compensation/CompareView.tsx
autonomous: true
requirements: [COMP-01, COMP-02]
must_haves:
  truths:
    - "User can enter one or more RSU/equity grants for the compare offer (grant name, shares, grant price, start date, vesting schedule)."
    - "User can add and remove RSU grants in the compare form before calculating."
    - "After calculating, the RSU (Annual) comparison row shows a non-$0 value for the compare offer when grants were entered."
    - "The RSU (Annual) delta reflects the real difference between primary and compare offers (not always $0 on the compare side)."
  artifacts:
    - path: "src/components/compensation/CompareView.tsx"
      provides: "RSU grant input UI in the compare form, wired into setComparePackage"
      contains: "rsuGrants"
  key_links:
    - from: "src/components/compensation/CompareView.tsx"
      to: "src/store/useCompensationStore.ts"
      via: "setComparePackage receives the locally collected RSUGrant[] instead of an empty array"
      pattern: "rsuGrants:\\s*compareRsuGrants"
    - from: "src/components/compensation/CompareView.tsx"
      to: "src/store/useCompensationStore.ts"
      via: "RSU (Annual) row already calls calcAnnualRSU(comparePackage, timeMode); grants now flow through"
      pattern: "calcAnnualRSU\\(comparePackage"
---

<objective>
Close the UAT gap on Test 3 (Compare View): the compare offer form has no RSU/equity input fields, so the "RSU (Annual)" comparison row always shows $0 for the compare offer. CompareView.tsx hardcodes `rsuGrants: []` at line 43.

Add an RSU/equity grant input section to the existing compare form in CompareView.tsx, manage the entered grants in local component state, and pass them into `setComparePackage` instead of the hardcoded empty array. Reuse the field set and vesting-preset UX from CompensationModal.tsx's Equity tab so the compare form feels consistent. The comparison table already renders the RSU (Annual) row via `calcAnnualRSU(comparePackage, timeMode)` — once real grants flow in, the row and its delta render correct non-zero numbers.

Purpose: Make the offer comparison complete — equity is a material part of a compensation package, and without it the RSU delta is meaningless (COMP-01, COMP-02).
Output: Updated CompareView.tsx with a working RSU grant sub-form (add/remove grants) wired into the compare package.
</objective>

<execution_context>
@C:/Users/misha/ledger/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/misha/ledger/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The component to fix:
@src/components/compensation/CompareView.tsx
# Reuse the Equity-tab RSU input pattern, vesting presets, and grant-list UX from here:
@src/components/compensation/CompensationModal.tsx
# RSUGrant / VestingSchedule / VestingPreset / VestingFrequency types, calcAnnualRSU, generateVestEvents:
@src/store/useCompensationStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add local RSU-grant state and add/remove handlers to the compare form</name>
  <files>src/components/compensation/CompareView.tsx</files>
  <action>
    Import the RSUGrant, VestingSchedule, VestingPreset, and VestingFrequency types from '../../store/useCompensationStore' (type-only import). Add local component state to CompareView to collect RSU grants for the compare offer:
    - compareRsuGrants: RSUGrant[] (the list of grants already added to the compare offer), initialized to an empty array.
    - Draft-input state for the grant currently being entered, mirroring CompensationModal's Equity tab field names but prefixed for clarity: rsuGrantName (string, ''), rsuGrantShares (string, ''), rsuGrantPrice (string, default the same value as compareCompanyCurrentPrice's initial '100'), rsuStartDate (string, default new Date().toISOString().split('T')[0]), rsuPreset (VestingPreset, default '4yr-1yr-cliff'), rsuTotalMonths (string, '48'), rsuCliffMonths (string, '12'), rsuFrequency (VestingFrequency, default 'monthly').

    Add a handler addCompareRsuGrant() that mirrors the schedule-building logic in CompensationModal.handleSaveRSU: guard against empty rsuGrantName / rsuGrantShares / rsuGrantPrice (return early if missing). Build a VestingSchedule where totalVestMonths is Number(rsuTotalMonths) when preset is 'custom', else 48 for '4yr-1yr-cliff' and 36 for '3yr-no-cliff'; cliffMonths is Number(rsuCliffMonths) when preset is 'custom', else 12 for '4yr-1yr-cliff' and 0 otherwise; frequency is rsuFrequency. Append a new RSUGrant ({ id: crypto.randomUUID(), grantName, grantShares: Number(...), grantPrice: Number(...), grantStartDate: rsuStartDate, vestingSchedule }) to compareRsuGrants, then reset rsuGrantName and rsuGrantShares to '' (leave price/date/preset for fast multi-grant entry).

    Add a handler removeCompareRsuGrant(id: string) that filters the grant out of compareRsuGrants.

    Do NOT touch the existing base/bonus/ESPP/RRSP state or handlers. Do NOT add a separate "Calculate" trigger for RSU — grants live in component state until the existing Calculate Comparison button fires (wired in Task 2). Keep all new state declarations grouped with the existing useState declarations near the top of the component.
  </action>
  <verify>
    <automated>grep -q "compareRsuGrants" src/components/compensation/CompareView.tsx && grep -q "addCompareRsuGrant" src/components/compensation/CompareView.tsx && grep -q "removeCompareRsuGrant" src/components/compensation/CompareView.tsx && grep -q "crypto.randomUUID" src/components/compensation/CompareView.tsx && echo OK</automated>
  </verify>
  <done>CompareView holds an RSUGrant[] in local state with working add and remove handlers that build a VestingSchedule identically to CompensationModal. TypeScript compiles (verified in Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Render the RSU input section in the compare form and wire grants into setComparePackage</name>
  <files>src/components/compensation/CompareView.tsx</files>
  <action>
    In the compare input form (the left column, currently lines 58-132, after the RRSP grid and before the Calculate Comparison button), add an RSU/Equity section using the existing inputClass and labelClass conventions so it visually matches the rest of the compare form. Include, in compact layout consistent with the surrounding 2-column grids:
    - A section heading/label "RSU / Equity Grant" (use labelClass or a small heading matching the form's existing h3 style — keep it subordinate to the form, not a new full heading).
    - Grant Name text input bound to rsuGrantName.
    - Number of Shares numeric input bound to rsuGrantShares.
    - Grant Price ($) numeric input bound to rsuGrantPrice and Grant Start Date date input bound to rsuStartDate (use a grid-cols-2 layout like the existing ESPP/RRSP rows).
    - Vesting preset selection: three buttons for '4yr-1yr-cliff' ("4yr / 1yr cliff"), '3yr-no-cliff' ("3yr / no cliff"), and 'custom' ("Custom"), styled like CompensationModal's preset buttons but sized to fit the narrow compare column (a select dropdown bound to rsuPreset is an acceptable alternative if it fits the column better — pick whichever matches the compare form's density). Include a frequency control (select bound to rsuFrequency with Monthly/Quarterly options).
    - When rsuPreset === 'custom', show Total Vest (months) input bound to rsuTotalMonths and Cliff (months) input bound to rsuCliffMonths.
    - An "Add Grant" button (type="button") that calls addCompareRsuGrant. Use a secondary button style (not the accent Calculate button) so it is visually distinct.
    - A list of already-added grants from compareRsuGrants, each row showing "{grantName} ({grantShares.toLocaleString()} shares)" and a Remove button (type="button") calling removeCompareRsuGrant(grant.id), styled like CompensationModal's Current Grants list. Render the list only when compareRsuGrants.length > 0.

    Then update the existing handleCalculate / setComparePackage call: replace the hardcoded `rsuGrants: []` (line 43) with `rsuGrants: compareRsuGrants`. Leave every other field in that object unchanged.

    Do NOT introduce a modal — keep this inline in the existing compare form column. Do NOT alter the comparison table on the right; it already reads calcAnnualRSU(comparePackage, timeMode) and will render correctly once real grants are passed in.
  </action>
  <verify>
    <automated>grep -q "rsuGrants: compareRsuGrants" src/components/compensation/CompareView.tsx && ! grep -q "rsuGrants: \[\]" src/components/compensation/CompareView.tsx && grep -q "value={rsuGrantShares}" src/components/compensation/CompareView.tsx && echo OK</automated>
  </verify>
  <done>The compare form renders RSU grant inputs (name, shares, price, start date, vesting preset, frequency, custom months when applicable), an Add Grant button, and a removable list of added grants. handleCalculate passes compareRsuGrants into setComparePackage instead of an empty array. The hardcoded `rsuGrants: []` no longer appears in the file.</done>
</task>

<task type="auto">
  <name>Task 3: Verify build, typecheck, and prove RSU calculation returns non-zero in-window</name>
  <files>src/components/compensation/CompareView.tsx, /tmp/verify-rsu.mjs</files>
  <action>
    First run the production build (which runs `tsc -b` first, then vite build) to confirm the new types, imports, and JSX compile with no errors. Resolve any TypeScript errors (e.g., missing type imports, RSUGrant field mismatches, VestingFrequency/VestingPreset union typing on select onChange handlers — cast e.target.value to the union type as CompensationModal does at its frequency/preset handlers).

    Then prove the behavioral outcome with an executable check, not prose. The phase goal is behavioral ("RSU row reflects real entered values instead of $0"), so the calculation must actually run and assert a non-zero result. CRITICAL timeMode caveat: calcAnnualRSU filters vest events by timeMode, and the store default is 'current-year' (useCompensationStore line 252). A grant whose start date is today with the default '4yr-1yr-cliff' preset has its first (cliff) vest land ~12 months out — i.e. NEXT calendar year — which 'current-year' mode filters OUT, yielding $0 even though the wiring is correct. The verification MUST therefore use inputs that demonstrably place at least one vest event inside the active timeMode window. Two acceptable ways to guarantee an in-window vest:
    - Use a grantStartDate roughly 12 months in the PAST so the cliff vest lands in the current calendar year (works with the 'current-year' default), OR
    - Pass timeMode 'next-1-year' with a near-future grantStartDate so the cliff vest at ~12 months falls inside the today..+1-year window.

    Write a throwaway ESM verification script at /tmp/verify-rsu.mjs that imports the real calcAnnualRSU (and generateVestEvents) from the compiled output or via tsx/vitest against the source, constructs a sample RSUGrant whose cliff vest is guaranteed in-window per the caveat above (e.g. 1000 shares, grantPrice irrelevant since vestValue uses companyCurrentPrice, vestingSchedule preset '4yr-1yr-cliff' totalVestMonths 48 cliffMonths 12 frequency 'monthly'), wraps it in a minimal CompensationPackage with companyCurrentPrice 100 and rsuGrants set to that grant, calls calcAnnualRSU(pkg, timeMode) with the matching timeMode, and asserts the returned number is > 0 — exiting non-zero (process.exit(1)) with a clear message if the assertion fails. Run it with `node /tmp/verify-rsu.mjs` (or `npx tsx /tmp/verify-rsu.mjs` if importing the .ts source directly so transpilation is handled). Choosing to import the source via tsx avoids a separate compile step and keeps the check self-contained. The script is throwaway and is NOT committed with the source change.

    The combination — build passes, and the runtime assertion fires with a real > 0 result for an in-window vest — proves the RSU (Annual) compare cell and delta render real, non-zero values once grants flow through setComparePackage.
  </action>
  <verify>
    <automated>npm run build && npx tsx /tmp/verify-rsu.mjs && echo "RSU CALC > 0 CONFIRMED"</automated>
  </verify>
  <done>`npm run build` completes with no TypeScript or Vite errors. The /tmp/verify-rsu.mjs runtime check imports the real calcAnnualRSU, feeds it a sample grant whose cliff vest falls inside the active timeMode window (per the timeMode caveat — past-dated grant for 'current-year', or 'next-1-year' mode for a near-future grant), and exits 0 only when calcAnnualRSU returns > 0 — proving the RSU (Annual) compare cell and delta render real, non-zero values rather than the previous flat $0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user form input → component state → store | Untrusted numeric/text input crosses into local state and the persisted Zustand store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | RSU numeric inputs (shares, price, months) in CompareView | accept | Inputs are coerced with Number(...) and the existing calculators (calcAnnualRSU/generateVestEvents) already guard with `|| 0` and Math.floor; NaN/empty coerces to 0 — no crash, no privilege. Client-only single-user PWA, no server trust. |
| T-06-02 | Information Disclosure | comparePackage in persisted localStorage | accept | Data is the user's own compensation figures on their own device; no new sensitive surface beyond what the primary package already persists. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages are installed in this plan; all types and calculators already exist in the repo. N/A by inspection. |
</threat_model>

<verification>
- `npm run build` (runs `tsc -b` then `vite build`) passes with no errors.
- Executable RSU check: `/tmp/verify-rsu.mjs` imports the real calcAnnualRSU, feeds it a sample grant whose cliff vest is guaranteed inside the active timeMode window, and asserts the result is > 0 (exits non-zero otherwise). This converts the load-bearing "RSU row shows non-$0" claim from prose into a runtime assertion.
- timeMode caveat (why the runtime check is necessary): calcAnnualRSU filters vest events by timeMode; the store default is 'current-year' (useCompensationStore line 252). A grant started today with the '4yr-1yr-cliff' default has its cliff vest ~12 months out = NEXT year, which 'current-year' filters out → $0 despite correct wiring. The verification therefore must use a past-dated grant (cliff lands this calendar year) for 'current-year', or 'next-1-year' mode for a near-future grant.
- Manual confirmation (runtime app): entering an RSU grant whose vest falls inside the active timeMode window and clicking Calculate Comparison produces a non-zero "RSU (Annual)" value for the compare offer and a delta that differs from the primary offer's RSU value.
- The string `rsuGrants: []` no longer appears in CompareView.tsx; `rsuGrants: compareRsuGrants` does.
- No changes to useCompensationStore.ts or CompensationModal.tsx (CompareView-only fix; /tmp/verify-rsu.mjs is throwaway and not committed).
</verification>

<success_criteria>
- The compare offer form contains RSU/equity input fields (grant name, shares, grant price, start date, vesting schedule preset + frequency, custom months) plus add and remove controls — matching the primary package's CompensationModal field set.
- Entered grants are stored in local state and passed to setComparePackage on Calculate; the previous hardcoded empty array is gone.
- The "RSU (Annual)" comparison row reflects real non-zero numbers for the compare offer and a correct delta versus the primary offer.
- Production build and typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/06-total-compensation-calculator/06-04-SUMMARY.md` when done
</output>
